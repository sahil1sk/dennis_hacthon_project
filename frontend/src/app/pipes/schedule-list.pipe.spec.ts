import { ScheduleListPipe } from './schedule-list.pipe';

describe('ScheduleListPipe', () => {
  it('create an instance', () => {
    const pipe = new ScheduleListPipe();
    expect(pipe).toBeTruthy();
  });
});
